import styled from 'styled-components';

const Nav = styled.div`
  display: flex;
  padding: 12px 22px;
  background-color:#fff;

  a {
    text-decoration: none;
    color:#616161;
  }

  .logo {
    display: flex;
    cursor: pointer;
  }
  `
export default Nav;
